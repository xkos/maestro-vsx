---
description: AI 决策边界框架 — 定义 AI 行为的三级决策体系和边界迭代机制
alwaysApply: true
---

# AI 决策边界框架

> 定义 AI 行为的三级决策体系和边界迭代机制。alwaysApply。

## 三级决策体系

所有 AI 的行为决策分为三级：

| 级别 | 含义 | AI 行为 |
|------|------|---------|
| Always do | 确定安全的操作 | 直接执行，不需要确认 |
| Ask first | 有副作用或不确定的操作 | 说明意图和影响，等人确认后再执行 |
| Never do | 硬性禁止的操作 | 绝对不做，即使用户要求也应提醒风险 |

## 如何判断一个操作属于哪一级

### Always do 的特征
- 做了不会产生不可逆的副作用
- 已有明确的规范或 task 要求
- 影响范围可预测且有限

### Ask first 的特征
- 可能影响其他模块或已有功能
- 涉及架构层面的变更
- 超出当前 task 定义的范围
- 不确定是否符合项目意图

### Never do 的特征
- 违反架构约束（如依赖方向）
- 修改不属于 AI 职责的内容（如 me2ai 文档）
- 改动生成代码或受外部工具管理的文件
- 项目中有明确的禁止规则

## 边界的来源

项目的具体 Boundary 规则定义在 `AGENTS.md` 的"AI 决策边界"章节中。

## 边界的迭代机制

Boundary 不是一次性定义的，而是随项目推进持续演化：

### 新增规则的触发条件
- AI 做了不该做的事 → 补充 Ask first 或 Never do
- AI 反复询问不需要问的事 → 提升为 Always do
- decisions.md 中的踩坑记录具有通用性 → 升级为 Boundary 规则
- 新模块/新技术引入带来新的约束 → 补充对应规则

### 调整规则的方式
- Ask first → Always do：经过多次实践确认安全后放宽
- Ask first → Never do：发现风险比预期大后收紧
- 规则过时（技术栈变更等）→ 删除或修改

### 记录变更
当 Boundary 规则变更时，在 `docs/ai2ai/decisions.md` 中记录变更原因，格式：

```markdown
### B001: [规则变更描述]
- 日期：...
- 变更：[旧规则] → [新规则]
- 原因：[触发变更的具体事件]
```
