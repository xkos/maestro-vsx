---
description: ai2ai 文档维护规则 — 定义 AI 在每次迭代中如何维护项目状态文档
alwaysApply: true
---

# ai2ai 文档维护规则

> AI 在每次迭代中如何维护项目状态文档。alwaysApply。

## 什么是 ai2ai

`docs/ai2ai/` 是 AI 自维护的项目状态文档，与人维护的 me2ai 文档互补。

- me2ai = "应该是什么样"（需求、架构、规范）
- ai2ai = "现在实际是什么样"（状态、进度、经验）

## 文件结构

```
docs/ai2ai/
├── status.md              — 全局状态快照（覆盖更新）
├── decisions.md           — 经验库：微决策 + 踩坑 + Boundary 变更（只追加）
├── tasks/                 — 迭代任务分解（事前计划）
│   └── NNN-xxx.md
└── iterations/            — 迭代归档（事后总结）
    └── NNN-xxx.md
```

tasks/ 和 iterations/ 共享编号，一一配对。

## 迭代工作流

### 1. 迭代开始前：生成 task 文件

创建 `docs/ai2ai/tasks/NNN-xxx.md`，内容包括：

```markdown
# 任务 NNN: [标题]

> 状态：🔨 进行中
> 配对迭代：[iterations/NNN-xxx.md](../iterations/NNN-xxx.md)

## 迭代目标
[一句话描述本次迭代要达成什么]

## 任务分解
- [ ] T1: [任务描述]
  - 验证：[具体的验证标准]
- [ ] T2: ...
```

任务分解原则：
- 每个任务是独立可实现的原子单元
- 每个任务有明确的验证标准（不是"实现 XX"，而是"XX 能做到 YY"）
- 任务按依赖顺序排列
- 单次迭代任务数建议 3-8 个，超过则拆分迭代

### 2. 迭代进行中：更新 task 状态

- 完成一个任务后将 `[ ]` 改为 `[x]`
- 如果任务需要调整（拆分、合并、放弃），直接在 task 文件中修改并标注原因

### 3. 迭代结束后：归档

1. **task 文件**：状态改为 `✅ 已关闭`
2. **新建 iteration 文件** `docs/ai2ai/iterations/NNN-xxx.md`：
   - 记录：目标、完成内容、未完成/遗留、回归影响、可测试项
   - 可测试项必须是具体的操作步骤 + 预期结果
   - "测试结果"部分留空，由人工填写
3. **更新 status.md**：
   - 更新"最后更新"日期
   - 更新"当前迭代"指向
   - 更新模块就绪度表格
   - 更新已知问题/技术债
4. **按需更新 decisions.md**

## status.md 中的迭代指向

```markdown
## 当前迭代

- 上一迭代：[NNN-xxx](tasks/NNN-xxx.md) ✅ 已关闭
- 当前活跃：[NNN+1-yyy](tasks/NNN+1-yyy.md) 🔨 进行中（M/N 任务完成）
```

## 什么不算迭代

- 纯文档更新、纯讨论、纯 bug 修复（除非修复涉及架构变更）不需要新建 task/iteration 文件
- 但如果 bug 修复改变了模块状态，需要更新 status.md

## 安全护栏

- **错误循环检测**：连续 3 次尝试相同修复未成功时，停止尝试，报告问题现象、已尝试的方案、可能的原因，等待人工介入
- **变更范围感知**：单次迭代修改文件超过 10 个时，暂停并确认是否需要拆分迭代

## 模块状态标记

| 标记 | 含义 |
|------|------|
| ✅ 可用 | 功能已实现且可正常使用 |
| 🔨 部分 | 部分功能已实现，或骨架已搭建 |
| 🔨 骨架 | 仅有接口/结构定义，无实际逻辑 |
| ❌ 空壳 | 文件存在但无实现 |
| ❌ 未开始 | 尚未创建 |

## 可测试项规范

每个可测试项必须包含：
- 具体的操作步骤（用户做什么）
- 预期结果（应该看到什么）
- 如果涉及已有功能，标注是否为回归测试

## 经验库记录类型

`docs/ai2ai/decisions.md` 中使用三种前缀：

| 前缀 | 类型 | 记录什么 |
|------|------|---------|
| D | 技术微决策 | 设计文档未覆盖的实现选型 |
| L | 踩坑记录 | 技术限制、意外行为、绕过方案 |
| B | Boundary 变更 | AI 决策边界规则的新增/调整 |
